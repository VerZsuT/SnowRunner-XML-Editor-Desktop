import engineTemplate from './engine'
import gearboxTemplate from './gearbox'
import suspensionTemplate from './suspension'
import trailerTemplate from './trailer'
import truckTemplate from './truck'
import wheelsTemplate from './wheels'
import winchTemplate from './winch'

export const templates = {
  engine: engineTemplate,
  gearbox: gearboxTemplate,
  suspension: suspensionTemplate,
  trailer: trailerTemplate,
  truck: truckTemplate,
  wheels: wheelsTemplate,
  winch: winchTemplate
}

export const extra = {}
