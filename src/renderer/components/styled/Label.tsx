import { InputLabel, styled } from "@mui/material";

export default styled(InputLabel)({
    color: "black",
    display: "inline-block",
    position: "relative",
    top: "8px",
    marginRight: "15px"
});
