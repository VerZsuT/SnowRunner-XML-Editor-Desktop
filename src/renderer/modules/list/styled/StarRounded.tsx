import { styled } from '@mui/system'
import { StarRounded as StarRoundedIcon } from '@mui/icons-material'

export default styled(StarRoundedIcon)({
    position: 'absolute',
    top: '5px',
    left: '5px'
})
