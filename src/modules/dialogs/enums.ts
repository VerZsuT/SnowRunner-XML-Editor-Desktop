/** Тип выбираемого элемента в диалоге. */
export enum DialogSourceType {
  /** Файл. */
  file = 'file',

  /** Папка. */
  dir = 'dir'
}

/** Тип диалога. */
export enum DialogType {
  /** Выбор. */
  open = 'open',

  /** Сохранение. */
  save = 'save'
}
