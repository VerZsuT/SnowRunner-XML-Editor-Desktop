/** Цветовые зоны поля ввода. */
type InputAreas = {
    /** Красная зона. */
    red?: [number, number][]

    /** Жёлтая зона. */
    yellow?: [number, number][]

    /** Зелёная зона. */
    green?: [number, number][]
}

export default InputAreas
