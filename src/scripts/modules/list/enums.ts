export enum SrcType {
    mods = 'mods',
    dlc = 'dlc',
    main = 'main'
}

export enum ListType {
    trucks = 'trucks',
    trailers = 'trailers',
    cargo = 'cargo'
}

export enum FromList {
    dlc = 'dlc',
    mods = 'mods'
}
